import 'package:flutter/material.dart';

class BopmPage extends StatelessWidget {
  const BopmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
    );
  }
}
